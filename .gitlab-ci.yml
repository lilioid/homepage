stages:
  - deploy

deploy:
  stage: deploy

  variables:
    dir_root: /var/www/
    webspace: finnthorben/

  script:
    - "target=$dir_root$webspace"
    - "echo \"Deploying to: $target\""
    - "rsync -rlvc --delete ./* $target"
    - "rsync -rlvc --delete -r ./.htaccess $target"